return {
  'williamboman/mason.nvim',
  dependencies = {
    'williamboman/mason-lspconfig.nvim',
    'neovim/nvim-lspconfig',  -- 添加这个依赖
  },
  config = function()
    require('mason').setup()
  end,
}